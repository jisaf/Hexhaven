
/**
 * HexTile Sprite Component
 *
 * Represents a single hex tile on the game board.
 * Supports a 'game' mode for gameplay and a 'design' mode for the scenario editor.
 */

import * as PIXI from 'pixi.js';
import { type Axial, axialToScreen, HEX_SIZE } from './hex-utils';
import type { TerrainType } from '../../../shared/types';

// ... (rest of the file is unchanged)
