# Local development URLs
# Commented out to allow auto-detection based on window.location
# VITE_API_URL=http://localhost:3000/api
# VITE_WS_URL=http://localhost:3000

# Backend port (optional, defaults to 3001 in development)
# Uncomment and change if backend runs on a different port
# VITE_BACKEND_PORT=3001
